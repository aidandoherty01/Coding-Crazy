import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette: {
        background: {
            default: "#0f172a", // Default background color
        },
        text: {
            primary: "#ffffff", 
        },
    },
});

export default theme;
